# Phase 0 - Baseline Snapshot

Date: 2026-02-14  
Repository root: `/Users/gokhancamci/AfetNet1`  
Branch: `main`  
Commit: `618dd5e`

## Snapshot Facts

- Worktree size: `202` modified/deleted/untracked entries (`git status --short | wc -l`).
- Core surface area:
  - `80` screen files (`src/core/screens`)
  - `172` service files (`src/core/services`)
  - `17` store files (`src/core/stores`)

## Build/Health Baseline

- `npm run -s typecheck` -> pass.
- `npm run -s healthcheck` -> pass, report generated:
  - `reports/e2e-report.md`

## External Readiness Flags (from health report)

Reference: `reports/e2e-report.md:14`

- Missing env markers: `AFAD_API_URL`, `APNS_*`, `FIREBASE_PROJECT_ID`, `FIREBASE_CLIENT_EMAIL`, `RC_IOS_KEY`, `POSTGRES_URL`, `DATABASE_URL`, etc.
- Backend health unresolved: `Public URL bulunamadı (Render)` (`reports/e2e-report.md:35`).
- Public pages unreachable in current health probe: privacy and terms HTTP 0.

## Crash Signal Baseline

The TestFlight screenshots show `error_<timestamp>_<rand>` identifiers.  
Those IDs are generated by global UI fallback:

- `src/core/components/ErrorBoundary.tsx:44`

Meaning: app is hitting unhandled runtime errors at React tree level, not only feature-level API errors.

## Baseline Risk Statement

1. Current codebase is functionally rich but operationally unstable under identity/fallback mismatch conditions.
2. Local static checks are green, but this does not guarantee runtime correctness in TestFlight flows.
3. High-trust release requires feature-level deterministic verification (especially SOS, messaging, family location/status).
